name: Cats and dogs CI/CD

on:
  push:
    branches:
      - ilyess/step04_eval

jobs:
  train:
    runs-on: ubuntu-latest
    steps:
      - name: Première step d’intro !!
        run: |
          echo "COUCOU !"
